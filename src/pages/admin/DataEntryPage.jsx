import { useEffect, useMemo, useState } from "react";
import { useAuth } from "../../auth/AuthProvider";
import { archiveFinancialRecord, fetchDataEntryOptions, fetchExistingFinancialPeriod, fetchFinancialRecordAcademicYears, fetchFinancialRecords, getAcademicYearFromMonth, getFinanceTermFromMonth, restoreFinancialRecord, saveFinancialRecords, updateFinancialRecord } from "../../lib/financialData";
import "./DataEntryPage.css";

const SCENARIOS=["Actual","Budget","Forecast"],PAGE_SIZE=25;
const MONTHS=[["01","January"],["02","February"],["03","March"],["04","April"],["05","May"],["06","June"],["07","July"],["08","August"],["09","September"],["10","October"],["11","November"],["12","December"]].map(([value,label])=>({value,label}));
const metricKey=m=>String(m?.code||m?.name||"").trim().toLowerCase(),metricLabel=m=>m?.name||m?.code||"Metric",isPercentageMetric=m=>metricKey(m)==="vat_rate"||metricLabel(m).trim().toLowerCase()==="vat rate",metricUnit=m=>isPercentageMetric(m)?"%":"AED";
function uniqueMetrics(a=[]){const s=new Set();return a.filter(m=>{const k=metricKey(m);if(!k||s.has(k))return false;s.add(k);return true;});}
function getYearOptions(){const y=new Date().getFullYear();return Array.from({length:12},(_,i)=>String(y+3-i));}
function formatRecordMonth(v){if(!v)return"—";return new Intl.DateTimeFormat("en-GB",{month:"short",year:"numeric"}).format(new Date(`${v}T00:00:00`));}
function formatMoney(v){return new Intl.NumberFormat("en-AE",{style:"currency",currency:"AED",maximumFractionDigits:2}).format(Number(v||0));}
function formatMetricValue(v,m){return isPercentageMetric(m)?`${Number(v||0).toLocaleString("en-AE",{maximumFractionDigits:2})}%`:formatMoney(v);}
function pages(c,t){if(t<=7)return Array.from({length:t},(_,i)=>i+1);const a=[1],s=Math.max(2,c-1),e=Math.min(t-1,c+1);if(s>2)a.push("s");for(let p=s;p<=e;p++)a.push(p);if(e<t-1)a.push("e");a.push(t);return a;}
function academicYearMonths(ay){if(!ay)return[];const match=String(ay).match(/AY(\d{4})-(\d{2})/);if(!match)return[];const start=Number(match[1]);return [[9,"September",start],[10,"October",start],[11,"November",start],[12,"December",start],[1,"January",start+1],[2,"February",start+1],[3,"March",start+1],[4,"April",start+1],[5,"May",start+1],[6,"June",start+1],[7,"July",start+1],[8,"August",start+1]].map(([m,label,y])=>({value:`${y}-${String(m).padStart(2,"0")}`,label:`${label} ${y}`}));}

export default function DataEntryPage({initialStreamCode="",initialView=""}){
 const {role}=useAuth(),canEdit=role==="admin"||role==="editor",canRestore=role==="admin";
 const fresh=()=>({schoolId:"",revenueStreamId:"",programmeId:"",reportingMonth:"",reportingYear:String(new Date().getFullYear()),scenario:"Actual"});
 const [mode,setMode]=useState(initialView==="register"?"manage":"new"),[options,setOptions]=useState({schools:[],revenueStreams:[],metrics:[],programmes:[]}),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[error,setError]=useState(""),[success,setSuccess]=useState(""),[form,setForm]=useState(fresh),[metricValues,setMetricValues]=useState({});
 const [periodChecking,setPeriodChecking]=useState(false),[existingPeriod,setExistingPeriod]=useState([]),[periodCheckError,setPeriodCheckError]=useState("");
 const [records,setRecords]=useState([]),[recordsLoading,setRecordsLoading]=useState(false),[recordsError,setRecordsError]=useState(""),[totalCount,setTotalCount]=useState(0),[availableAcademicYears,setAvailableAcademicYears]=useState([]);
 const [recordFilters,setRecordFilters]=useState({schoolId:"",revenueStreamId:"",metricCode:"",academicYear:"",month:"",scenario:"",category:"",programmeId:"",search:"",includeDeleted:false}),[currentPage,setCurrentPage]=useState(1),[editingRecord,setEditingRecord]=useState(null),[editAmount,setEditAmount]=useState(""),[recordActionLoading,setRecordActionLoading]=useState(false),[deleteRecord,setDeleteRecord]=useState(null);
 const yearOptions=useMemo(getYearOptions,[]);
 useEffect(()=>{let active=true;(async()=>{try{setLoading(true);const [d,years]=await Promise.all([fetchDataEntryOptions(),fetchFinancialRecordAcademicYears()]);if(active){setOptions(d);setAvailableAcademicYears(years);if(initialStreamCode){const target=d.revenueStreams.find(s=>String(s.code).toLowerCase()===String(initialStreamCode).toLowerCase());if(target){setRecordFilters(c=>({...c,revenueStreamId:String(target.id)}));setForm(c=>({...c,revenueStreamId:String(target.id)}));}}}}catch(e){if(active)setError(e?.message||"Unable to load the financial records workspace.");}finally{if(active)setLoading(false);}})();return()=>{active=false;};},[initialStreamCode]);
 useEffect(()=>{setMode(initialView==="register"?"manage":"new");},[initialView]);
 const selectedSchool=options.schools.find(s=>String(s.id)===String(form.schoolId)),selectedStream=options.revenueStreams.find(s=>String(s.id)===String(form.revenueStreamId)),selectedProgramme=options.programmes.find(p=>String(p.id)===String(form.programmeId)),isLeasing=selectedStream?.code==="leasing",streamMetrics=options.metrics.filter(m=>String(m.revenue_stream_id)===String(form.revenueStreamId));
 const managementStream=options.revenueStreams.find(s=>String(s.id)===String(recordFilters.revenueStreamId)),showManagementProgramme=managementStream?.code==="leasing";
 const managementMetrics=useMemo(()=>uniqueMetrics(recordFilters.revenueStreamId?options.metrics.filter(m=>String(m.revenue_stream_id)===String(recordFilters.revenueStreamId)):options.metrics).sort((a,b)=>metricLabel(a).localeCompare(metricLabel(b))),[options.metrics,recordFilters.revenueStreamId]);
 const leasingCategories=useMemo(()=>[...new Set(options.programmes.map(p=>p.category).filter(Boolean))].sort(),[options.programmes]);
 const leasingProgrammes=useMemo(()=>options.programmes.filter(p=>!recordFilters.category||p.category===recordFilters.category),[options.programmes,recordFilters.category]);
 const monthOptions=useMemo(()=>academicYearMonths(recordFilters.academicYear),[recordFilters.academicYear]);
 const combinedMonth=form.reportingYear&&form.reportingMonth?`${form.reportingYear}-${form.reportingMonth}`:"",academicYear=getAcademicYearFromMonth(combinedMonth),term=getFinanceTermFromMonth(combinedMonth),periodReady=Boolean(form.schoolId&&form.revenueStreamId&&combinedMonth&&(!isLeasing||form.programmeId)),periodLocked=existingPeriod.length>0;
 const selectedMetricIds=useMemo(()=>recordFilters.metricCode?options.metrics.filter(m=>metricKey(m)===recordFilters.metricCode&&(!recordFilters.revenueStreamId||String(m.revenue_stream_id)===String(recordFilters.revenueStreamId))).map(m=>m.id):[],[options.metrics,recordFilters.metricCode,recordFilters.revenueStreamId]);
 const categoryProgrammeIds=useMemo(()=>showManagementProgramme&&recordFilters.category&&!recordFilters.programmeId?options.programmes.filter(p=>p.category===recordFilters.category).map(p=>p.id):[],[showManagementProgramme,recordFilters.category,recordFilters.programmeId,options.programmes]);
 const searchResults=useMemo(()=>{const q=recordFilters.search.trim().toLowerCase();if(!q)return records;return records.filter(r=>[r.school?.name,r.school?.code,r.programme?.name,r.programme?.provider_name,r.revenue_stream?.name,r.metric?.name,r.academic_year,r.term,r.scenario].filter(Boolean).join(" ").toLowerCase().includes(q));},[records,recordFilters.search]);
 const usingSearch=Boolean(recordFilters.search.trim()),displayRecords=usingSearch?searchResults.slice((currentPage-1)*PAGE_SIZE,currentPage*PAGE_SIZE):records,displayCount=usingSearch?searchResults.length:totalCount,totalPages=Math.max(1,Math.ceil(displayCount/PAGE_SIZE)),paginationItems=pages(currentPage,totalPages),pageStart=displayCount?(currentPage-1)*PAGE_SIZE+1:0,pageEnd=Math.min(currentPage*PAGE_SIZE,displayCount);
 useEffect(()=>{let active=true;setExistingPeriod([]);setPeriodCheckError("");if(!periodReady)return;const timer=setTimeout(async()=>{try{setPeriodChecking(true);const rows=await fetchExistingFinancialPeriod({schoolId:form.schoolId,revenueStreamId:form.revenueStreamId,programmeId:isLeasing?form.programmeId:"",month:combinedMonth,scenario:form.scenario});if(active)setExistingPeriod(rows)}catch(e){if(active)setPeriodCheckError(e?.message||"Unable to check this reporting period.")}finally{if(active)setPeriodChecking(false)}},180);return()=>{active=false;clearTimeout(timer)};},[periodReady,form.schoolId,form.revenueStreamId,form.programmeId,form.scenario,combinedMonth,isLeasing]);
 async function loadRecords(){try{setRecordsLoading(true);setRecordsError("");const result=await fetchFinancialRecords({schoolId:recordFilters.schoolId,revenueStreamId:recordFilters.revenueStreamId,academicYear:recordFilters.academicYear,month:recordFilters.month,scenario:recordFilters.scenario,metricIds:selectedMetricIds,programmeId:showManagementProgramme?recordFilters.programmeId:"",programmeIds:categoryProgrammeIds,includeDeleted:recordFilters.includeDeleted,page:currentPage,pageSize:PAGE_SIZE,fetchAll:usingSearch});setRecords(result.records);setTotalCount(result.totalCount)}catch(e){setRecordsError(e?.message||"Unable to load financial records.")}finally{setRecordsLoading(false)}}
 useEffect(()=>{if(mode==="manage")loadRecords();},[mode,currentPage,recordFilters.schoolId,recordFilters.revenueStreamId,recordFilters.metricCode,recordFilters.academicYear,recordFilters.month,recordFilters.scenario,recordFilters.category,recordFilters.programmeId,recordFilters.includeDeleted,recordFilters.search]);
 function updateForm(n,v){setSuccess("");setError("");setForm(c=>({...c,[n]:v,...(n==="revenueStreamId"?{programmeId:""}:{})}));if(n==="revenueStreamId")setMetricValues({})}
 function filter(n,v){setCurrentPage(1);setRecordFilters(c=>({...c,[n]:v,...(n==="revenueStreamId"?{metricCode:"",category:"",programmeId:""}:{}),...(n==="academicYear"?{month:""}:{}),...(n==="category"?{programmeId:""}:{})}))}
 function reset(){setForm(fresh());setMetricValues({});setSuccess("");setError("");setExistingPeriod([]);setPeriodCheckError("")}
 function openExisting(){setRecordFilters(c=>({...c,schoolId:form.schoolId,revenueStreamId:form.revenueStreamId,academicYear,month:combinedMonth,scenario:form.scenario,programmeId:isLeasing?form.programmeId:"",search:""}));setCurrentPage(1);setMode("manage")}
 async function submit(e){e.preventDefault();if(!canEdit)return setError("Your account does not have permission to modify financial records.");if(periodChecking)return setError("Please wait while the reporting period is checked.");if(periodLocked)return setError("This reporting period already contains financial data. Open the existing record to make changes.");if(!form.schoolId||!form.revenueStreamId||!form.reportingMonth||!form.reportingYear)return setError("Select a school, revenue stream, reporting month and year.");if(isLeasing&&!form.programmeId)return setError("Select a programme for Leasing financial records.");try{setSaving(true);setError("");setSuccess("");const r=await saveFinancialRecords({schoolId:form.schoolId,revenueStreamId:form.revenueStreamId,programmeId:form.programmeId,month:combinedMonth,scenario:form.scenario,metricValues});setSuccess(`${r.savedCount} ${r.savedCount===1?"record":"records"} saved successfully.`);setExistingPeriod(await fetchExistingFinancialPeriod({schoolId:form.schoolId,revenueStreamId:form.revenueStreamId,programmeId:isLeasing?form.programmeId:"",month:combinedMonth,scenario:form.scenario}))}catch(e){setError(e?.message||"Unable to save the financial records.")}finally{setSaving(false)}}
 async function saveEdit(){try{setRecordActionLoading(true);await updateFinancialRecord(editingRecord.id,{amount:editAmount});setEditingRecord(null);await loadRecords()}catch(e){setRecordsError(e?.message||"Unable to update the financial record.")}finally{setRecordActionLoading(false)}}
 async function archive(){try{setRecordActionLoading(true);await archiveFinancialRecord(deleteRecord.id);setDeleteRecord(null);await loadRecords()}catch(e){setRecordsError(e?.message||"Unable to archive the financial record.")}finally{setRecordActionLoading(false)}}
 async function restore(r){try{setRecordActionLoading(true);await restoreFinancialRecord(r.id);await loadRecords()}catch(e){setRecordsError(e?.message||"Unable to restore the financial record.")}finally{setRecordActionLoading(false)}}
 if(loading)return <section className="data-entry-page"><div className="data-entry-loading">Loading Financial Records…</div></section>;
 return <section className="data-entry-page"><div className="data-entry-intro"><div><span className="data-entry-eyebrow">FINANCIAL RECORDS</span><h2>Commercial Data Workspace</h2><p>Enter new monthly results, find historical records and maintain commercial data safely.</p></div><div className="data-entry-status"><span className="status-dot"/>Supabase connected</div></div><div className="records-mode-switch"><button type="button" className={mode==="new"?"active":""} onClick={()=>setMode("new")}>＋ Enter Data</button><button type="button" className={mode==="manage"?"active":""} onClick={()=>setMode("manage")}>⌕ Find Records</button></div>{!canEdit&&<div className="data-entry-permission-banner">You currently have read-only access.</div>}{mode==="new"?<form className="data-entry-workspace" onSubmit={submit}><section className="data-entry-main-card"><div className="entry-card-header"><div><span className="entry-step">REPORTING PERIOD</span><h3>Monthly Financial Entry</h3><p>Select the school, revenue stream and period. Existing periods are protected from accidental replacement.</p></div></div><div className="entry-fields-grid"><label className="entry-field"><span>School</span><select value={form.schoolId} onChange={e=>updateForm("schoolId",e.target.value)}><option value="">Select school</option>{options.schools.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="entry-field"><span>Revenue Stream</span><select value={form.revenueStreamId} onChange={e=>updateForm("revenueStreamId",e.target.value)}><option value="">Select revenue stream</option>{options.revenueStreams.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label>{isLeasing&&<label className="entry-field"><span>Programme</span><select value={form.programmeId} onChange={e=>updateForm("programmeId",e.target.value)}><option value="">Select programme</option>{options.programmes.map(p=><option key={p.id} value={p.id}>{p.name}{p.provider_name?` — ${p.provider_name}`:""}</option>)}</select></label>}<div className="reporting-period-group"><label className="entry-field"><span>Month</span><select value={form.reportingMonth} onChange={e=>updateForm("reportingMonth",e.target.value)}><option value="">Select month</option>{MONTHS.map(m=><option key={m.value} value={m.value}>{m.label}</option>)}</select></label><label className="entry-field"><span>Year</span><select value={form.reportingYear} onChange={e=>updateForm("reportingYear",e.target.value)}>{yearOptions.map(y=><option key={y}>{y}</option>)}</select></label></div><label className="entry-field"><span>Scenario</span><select value={form.scenario} onChange={e=>updateForm("scenario",e.target.value)}>{SCENARIOS.map(s=><option key={s}>{s}</option>)}</select></label></div><div className="period-preview"><div><span>Academic Year</span><strong>{academicYear||"—"}</strong></div><div className="period-divider"/><div><span>Finance Term</span><strong>{term||"—"}</strong></div><div className="period-divider"/><div><span>Status</span><strong>{periodChecking?"Checking…":periodLocked?"Already reported":periodReady?"Available for entry":"Select period"}</strong></div></div>{periodCheckError&&<div className="entry-message error">{periodCheckError}</div>}{periodLocked&&<div className="existing-period-card"><div><strong>This period has already been reported</strong><p>{formatRecordMonth(`${combinedMonth}-01`)} · {selectedSchool?.name} · {selectedStream?.name}</p><button type="button" className="record-action" onClick={openExisting}>View / Edit Existing Record</button></div></div>}<div className="metrics-section"><div className="metrics-heading"><div><span className="entry-step">VALUES</span><h3>Financial Metrics</h3></div>{selectedStream&&<span className="selected-stream-pill">{selectedStream.name}</span>}</div>{!form.revenueStreamId?<div className="metric-placeholder"><strong>Select a revenue stream</strong></div>:periodLocked?<div className="metric-placeholder"><strong>Existing data is protected</strong><span>Use Find Records to review or edit this reporting period.</span></div>:<div className="metric-input-grid">{streamMetrics.map(m=><label className="metric-entry-card" key={m.id}><span>{metricLabel(m)}</span><div className="money-input"><span>{metricUnit(m)}</span><input type="number" min="0" step="0.01" value={metricValues[m.id]??""} onChange={e=>setMetricValues(c=>({...c,[m.id]:e.target.value}))}/></div></label>)}</div>}</div></section><aside className="data-entry-review-card"><div className="review-header"><span className="entry-step">REVIEW</span><h3>Entry Summary</h3></div><div className="review-list"><div><span>School</span><strong>{selectedSchool?.name||"—"}</strong></div><div><span>Revenue Stream</span><strong>{selectedStream?.name||"—"}</strong></div>{isLeasing&&<div><span>Programme</span><strong>{selectedProgramme?.name||"—"}</strong></div>}<div><span>Academic Year</span><strong>{academicYear||"—"}</strong></div><div><span>Term</span><strong>{term||"—"}</strong></div><div><span>Scenario</span><strong>{form.scenario}</strong></div></div>{error&&<div className="entry-message error">{error}</div>}{success&&<div className="entry-message success"><strong>✓ Saved successfully</strong><br/>{success}</div>}<div className="entry-actions"><button type="button" className="entry-reset-button" onClick={reset}>Clear</button><button type="submit" className="entry-save-button" disabled={!canEdit||saving||periodChecking||periodLocked}>{saving?"Saving…":periodLocked?"Already Reported":"Save Results"}</button></div><p className="entry-save-note">New entries only. Existing monthly results must be changed through Find Records.</p></aside></form>:<section className="manage-records-card"><div className="manage-records-header"><div><span className="entry-step">FINANCIAL ARCHIVE</span><h3>Find Financial Records</h3><p>Search or filter historical records when you need to review or change previously reported data.</p></div><span className="record-count-badge">{displayCount.toLocaleString()} records</span></div><div className="record-filter-grid"><label className="entry-field"><span>Search</span><input value={recordFilters.search} onChange={e=>filter("search",e.target.value)} placeholder="School, programme, stream…"/></label><label className="entry-field"><span>School</span><select value={recordFilters.schoolId} onChange={e=>filter("schoolId",e.target.value)}><option value="">All Schools</option>{options.schools.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="entry-field"><span>Revenue Stream</span><select value={recordFilters.revenueStreamId} onChange={e=>filter("revenueStreamId",e.target.value)}><option value="">All Streams</option>{options.revenueStreams.map(s=><option key={s.id} value={s.id}>{s.name}</option>)}</select></label><label className="entry-field"><span>Academic Year</span><select value={recordFilters.academicYear} onChange={e=>filter("academicYear",e.target.value)}><option value="">All Years</option>{availableAcademicYears.map(y=><option key={y}>{y}</option>)}</select></label><label className="entry-field"><span>Month</span><select value={recordFilters.month} onChange={e=>filter("month",e.target.value)} disabled={!recordFilters.academicYear}><option value="">All Months</option>{monthOptions.map(m=><option key={m.value} value={m.value}>{m.label}</option>)}</select></label><label className="entry-field"><span>Scenario</span><select value={recordFilters.scenario} onChange={e=>filter("scenario",e.target.value)}><option value="">All</option>{SCENARIOS.map(s=><option key={s}>{s}</option>)}</select></label><label className="archive-toggle"><input type="checkbox" checked={recordFilters.includeDeleted} onChange={e=>filter("includeDeleted",e.target.checked)}/>Archived</label></div>{showManagementProgramme&&<div className="record-filter-grid secondary"><label className="entry-field"><span>Category</span><select value={recordFilters.category} onChange={e=>filter("category",e.target.value)}><option value="">All Categories</option>{leasingCategories.map(c=><option key={c}>{c}</option>)}</select></label><label className="entry-field"><span>Programme</span><select value={recordFilters.programmeId} onChange={e=>filter("programmeId",e.target.value)}><option value="">All Programmes</option>{leasingProgrammes.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="entry-field"><span>Metric</span><select value={recordFilters.metricCode} onChange={e=>filter("metricCode",e.target.value)}><option value="">All Metrics</option>{managementMetrics.map(m=><option key={metricKey(m)} value={metricKey(m)}>{metricLabel(m)}</option>)}</select></label></div>}{recordsError&&<div className="entry-message error manage-message">{recordsError}</div>}{recordsLoading?<div className="records-loading">Searching financial records…</div>:!displayRecords.length?<div className="records-empty"><strong>No matching records</strong><span>Adjust the search or filters to find historical data.</span></div>:<><div className="records-table-wrap"><table className="records-table"><thead><tr><th>School</th><th>Stream / Programme</th><th>Period</th><th>Metric</th><th>Scenario</th><th>Amount</th><th>Status</th><th/></tr></thead><tbody>{displayRecords.map(r=><tr key={r.id} className={r.is_deleted?"archived-row":""}><td><strong>{r.school?.short_name||r.school?.code||r.school?.name||"—"}</strong><span>{r.school?.name}</span></td><td><strong>{r.revenue_stream?.name||"—"}</strong>{r.programme&&<span>{r.programme.name}</span>}</td><td><strong>{formatRecordMonth(r.month)}</strong><span>{r.academic_year} · {r.term}</span></td><td>{r.metric?.name||r.metric?.code||"—"}</td><td><span className={`scenario-pill ${String(r.scenario).toLowerCase()}`}>{r.scenario}</span></td><td className="record-amount">{formatMetricValue(r.amount,r.metric)}</td><td><span className={`record-status ${r.is_deleted?"archived":"active"}`}>{r.is_deleted?"Archived":"Active"}</span></td><td className="record-actions-cell"><div className="record-actions">{r.is_deleted?canRestore&&<button type="button" className="record-action restore" onClick={()=>restore(r)} disabled={recordActionLoading}>Restore</button>:canEdit&&<><button type="button" className="record-action" onClick={()=>{setEditingRecord(r);setEditAmount(String(r.amount??""))}}>Edit</button><button type="button" className="record-action danger" onClick={()=>setDeleteRecord(r)}>Archive</button></>}</div></td></tr>)}</tbody></table></div><div className="records-pagination"><span className="pagination-summary">Showing <strong>{pageStart}–{pageEnd}</strong> of <strong>{displayCount.toLocaleString()}</strong></span><div className="pagination-controls"><button type="button" className="pagination-arrow" disabled={currentPage<=1} onClick={()=>setCurrentPage(p=>p-1)}>‹</button>{paginationItems.map((p,i)=>typeof p==="number"?<button type="button" key={p} className={`pagination-page ${p===currentPage?"active":""}`} onClick={()=>setCurrentPage(p)}>{p}</button>:<span className="pagination-ellipsis" key={`${p}-${i}`}>…</span>)}<button type="button" className="pagination-arrow" disabled={currentPage>=totalPages} onClick={()=>setCurrentPage(p=>p+1)}>›</button></div></div></>}</section>}{editingRecord&&<div className="record-modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setEditingRecord(null)}}><div className="record-modal"><div className="record-modal-header"><div><span className="entry-step">EDIT EXISTING RECORD</span><h3>{editingRecord.metric?.name||"Financial Record"}</h3><p>Changes here update the previously reported value.</p></div><button className="modal-close" onClick={()=>setEditingRecord(null)}>×</button></div><div className="edit-record-context"><div><span>School</span><strong>{editingRecord.school?.name}</strong></div><div><span>Stream</span><strong>{editingRecord.revenue_stream?.name}</strong></div><div><span>Period</span><strong>{formatRecordMonth(editingRecord.month)}</strong></div><div><span>Scenario</span><strong>{editingRecord.scenario}</strong></div></div><label className="entry-field edit-amount-field"><span>Amount</span><div className="edit-money-input"><span className="edit-currency">{metricUnit(editingRecord.metric)}</span><input type="number" min="0" step="0.01" value={editAmount} onChange={e=>setEditAmount(e.target.value)}/></div></label><div className="record-modal-actions"><button className="entry-reset-button" onClick={()=>setEditingRecord(null)}>Cancel</button><button className="entry-save-button" onClick={saveEdit} disabled={recordActionLoading}>{recordActionLoading?"Saving…":"Save Changes"}</button></div></div></div>}{deleteRecord&&<div className="record-modal-backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)setDeleteRecord(null)}}><div className="record-modal archive-modal"><div className="archive-icon">!</div><h3>Archive financial record?</h3><p>This removes the record from active reporting without permanently deleting its history.</p><div className="archive-record-summary"><strong>{deleteRecord.school?.name} · {deleteRecord.revenue_stream?.name}</strong><span>{formatRecordMonth(deleteRecord.month)} · {deleteRecord.metric?.name} · {formatMetricValue(deleteRecord.amount,deleteRecord.metric)}</span></div><div className="record-modal-actions"><button className="entry-reset-button" onClick={()=>setDeleteRecord(null)}>Cancel</button><button className="archive-confirm-button" onClick={archive} disabled={recordActionLoading}>{recordActionLoading?"Archiving…":"Archive Record"}</button></div></div></div>}</section>;
}
