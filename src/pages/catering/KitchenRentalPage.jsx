import { useEffect, useMemo, useState } from "react";
import { useOutletContext } from "react-router-dom";
import { Bar, BarChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { fetchKitchenRentalRecords } from "../../lib/kitchenRentalData";
import "./KitchenRentalPage.css";

const SUPPLIER_BY_YEAR={"AY2025-26":"Ginza","AY2026-27":"Ben's Farmhouse"};
function formatCurrency(value){return new Intl.NumberFormat("en-AE",{style:"currency",currency:"AED",maximumFractionDigits:0}).format(Number(value||0));}
function formatCompactCurrency(value){return new Intl.NumberFormat("en-AE",{style:"currency",currency:"AED",notation:"compact",maximumFractionDigits:1}).format(Number(value||0));}
export default function KitchenRentalPage(){const{setHeaderControls}=useOutletContext();const[records,setRecords]=useState([]),[loading,setLoading]=useState(true),[loadError,setLoadError]=useState("");useEffect(()=>{setHeaderControls(null);return()=>setHeaderControls(null)},[setHeaderControls]);useEffect(()=>{let active=true;(async()=>{try{setLoading(true);setLoadError("");const data=await fetchKitchenRentalRecords();if(active)setRecords(data)}catch(error){console.error("Unable to load Kitchen Rental records from Supabase",error);if(active)setLoadError("Unable to load Kitchen Rental data from Supabase.")}finally{if(active)setLoading(false)}})();return()=>{active=false}},[]);const history=useMemo(()=>records.map((item,index)=>({...item,supplier:SUPPLIER_BY_YEAR[item.academicYear]||"—",status:index===records.length-1?"Current":"Previous"})),[records]);const current=history[history.length-1],previous=history.length>1?history[history.length-2]:null;if(loading)return <section className="kitchen-rental-page"><div className="kitchen-rental-panel">Loading Kitchen Rental data…</div></section>;if(loadError)return <section className="kitchen-rental-page"><div className="kitchen-rental-panel">{loadError}</div></section>;if(!current)return <section className="kitchen-rental-page"><div className="kitchen-rental-panel">No Kitchen Rental records have been entered yet. Add the annual rental record in Financial Records.</div></section>;const change=previous?.annualRent?((current.annualRent-previous.annualRent)/previous.annualRent)*100:0,vatAmount=current.annualRent*(current.vatRate/100);return <section className="kitchen-rental-page"><section className="kitchen-rental-kpis"><article className="kitchen-rental-kpi primary"><span>Annual Rental Income</span><strong>{formatCurrency(current.annualRent)}</strong><small>+ {current.vatRate}% VAT ({formatCurrency(vatAmount)})</small></article><article className="kitchen-rental-kpi"><span>Current Supplier</span><strong>{current.supplier}</strong><small>Current catering services agreement</small></article><article className="kitchen-rental-kpi"><span>School</span><strong>{current.school}</strong><small>{current.schoolCode}</small></article><article className="kitchen-rental-kpi"><span>Rental Change</span><strong className={change<0?"negative":"positive"}>{previous?`${change.toFixed(1)}%`:"—"}</strong><small>{previous?`vs ${formatCurrency(previous.annualRent)} previous rent`:"No prior annual record"}</small></article></section><section className="kitchen-rental-main-grid"><article className="kitchen-rental-panel performance-panel"><div className="kitchen-rental-heading"><div><span>Performance</span><h2>Annual Rental Comparison</h2><p>Contracted annual kitchen rental income, excluding VAT.</p></div><span className="kitchen-rental-pill">{current.schoolCode}</span></div><div className="kitchen-rental-chart"><ResponsiveContainer width="100%" height="100%"><BarChart data={history} margin={{top:16,right:20,left:10,bottom:8}}><CartesianGrid strokeDasharray="2 6" vertical={false} stroke="#e5e7eb"/><XAxis dataKey="academicYear" axisLine={false} tickLine={false} tick={{fontSize:12}}/><YAxis axisLine={false} tickLine={false} tick={{fontSize:11}} tickFormatter={formatCompactCurrency}/><Tooltip formatter={value=>formatCurrency(value)}/><Bar dataKey="annualRent" name="Annual Rent" fill="#159f8c" radius={[7,7,0,0]} maxBarSize={90}/></BarChart></ResponsiveContainer></div></article><article className="kitchen-rental-panel current-terms-panel"><div className="kitchen-rental-heading compact"><div><span>Current arrangement</span><h2>Rental Terms</h2></div><span className="kitchen-rental-status">Current</span></div><dl className="kitchen-rental-terms"><div><dt>Academic year</dt><dd>{current.academicYear}</dd></div><div><dt>Supplier</dt><dd>{current.supplier}</dd></div><div><dt>School</dt><dd>{current.school}</dd></div><div><dt>Annual rent</dt><dd>{formatCurrency(current.annualRent)}</dd></div><div><dt>VAT</dt><dd>{current.vatRate}% · {formatCurrency(vatAmount)}</dd></div><div><dt>Gross amount incl. VAT</dt><dd>{formatCurrency(current.annualRent+vatAmount)}</dd></div><div><dt>Commercial context</dt><dd>Catering Services Agreement</dd></div></dl><p className="kitchen-rental-note">Annual rent and VAT are maintained in Financial Records. Kitchen Rental remains an independent financial stream while operationally linked to Catering.</p></article></section><article className="kitchen-rental-panel history-panel"><div className="kitchen-rental-heading"><div><span>History</span><h2>Rental History</h2><p>Annual contracted rental values held in Supabase.</p></div></div><div className="kitchen-rental-table-wrap"><table className="kitchen-rental-table"><thead><tr><th>Academic Year</th><th>Arrangement</th><th>School</th><th>Supplier</th><th className="numeric">Annual Rent</th><th className="numeric">VAT</th><th className="numeric">Gross incl. VAT</th></tr></thead><tbody>{history.map(item=>{const vat=item.annualRent*item.vatRate/100;return <tr key={`${item.academicYear}-${item.schoolCode}`}><td><strong>{item.academicYear}</strong></td><td><span className={`history-status ${item.status.toLowerCase()}`}>{item.status}</span></td><td>{item.school}</td><td><strong>{item.supplier}</strong></td><td className="numeric">{formatCurrency(item.annualRent)}</td><td className="numeric">{item.vatRate}%</td><td className="numeric">{formatCurrency(item.annualRent+vat)}</td></tr>})}</tbody></table></div></article></section>}
